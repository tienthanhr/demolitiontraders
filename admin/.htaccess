# Allow access to admin directory (override server default if possible)
<IfModule mod_authz_core.c>
    Require all granted
</IfModule>

# Fallback for older Apache
<IfModule !mod_authz_core.c>
    Order allow,deny
    Allow from all
</IfModule>

# Prevent directory listing
Options -Indexes
