## Prevent direct access to frontend/admin copies
RewriteEngine On

# Deny any request to files under /frontend/admin/ - these are the development copies
RewriteRule ^ - [F,L]

# If a request somehow reaches here, return 403 to prevent exposing admin UI from frontend folder
ErrorDocument 403 "Access Denied"
